import React from "react";
import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";

const rootElement = document.getElementById("root");
if (rootElement) {
  const root = createRoot(rootElement);
  root.render(React.createElement(App));
} else {
  console.error("Root element not found");
  // Fallback: crear contenido directamente
  document.body.innerHTML = `
    <div style="
      min-height: 100vh;
      background-color: #111827;
      color: white;
      font-family: Arial, sans-serif;
      padding: 20px;
      text-align: center;
    ">
      <h1 style="color: #10b981; font-size: 48px; margin: 20px 0;">🚀 Pablex</h1>
      <p style="color: #9ca3af; font-size: 18px;">Tu Exchange de Criptomonedas</p>
      <div style="
        background-color: #1f2937;
        padding: 30px;
        border-radius: 12px;
        margin: 20px auto;
        max-width: 600px;
      ">
        <h2 style="color: #10b981; font-size: 28px; margin: 0 0 20px 0;">¡App Funcionando!</h2>
        <button onclick="window.location.href='/api/login'" style="
          background-color: #10b981;
          color: white;
          padding: 15px 40px;
          font-size: 18px;
          border: none;
          border-radius: 8px;
          cursor: pointer;
          font-weight: bold;
        ">🔐 Iniciar Sesión</button>
      </div>
    </div>
  `;
}